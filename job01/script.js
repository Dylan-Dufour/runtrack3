function citation() { // Fonction pour afficher une citation
    // Récupérer le contenu de l'élément ayant l'id "citation"
    const texte = document.getElementById("citation").textContent; // Récupération du texte
    
    // Afficher le contenu dans la console
    console.log(texte);
}